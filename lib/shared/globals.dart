import 'package:flutter/animation.dart';

class Globals {
  // la IPV4 debe ser la de tu computadora usando ipconfig desde el cmd
  static const baseApiUrl = 'http://192.168.0.12:8080/api/v1';
  static Color primaryColor = const Color.fromARGB(255, 22, 29, 47);
}